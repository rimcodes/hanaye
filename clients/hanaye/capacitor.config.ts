import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rimcodes.hanaye',
  appName: 'ionic-app-base',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
